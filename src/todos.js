const todoItem = (title, description, duedate, priority, isComplete, id) => {
    return {
        title,
        description,
        duedate,
        priority,
        isComplete,
        id
    }
}